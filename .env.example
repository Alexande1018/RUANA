# Gunicorn (Cloud Run / producción): workers, timeout y threads del proceso web.
# WEB_CONCURRENCY: número de workers (por defecto (2 × CPUs) + 1, o 2 si no se detectan CPUs).
# GUNICORN_TIMEOUT: segundos antes de matar un worker atascado (por defecto 30).
# GUNICORN_THREADS: threads por worker (por defecto 4).
# WEB_CONCURRENCY=2
# GUNICORN_TIMEOUT=30
# GUNICORN_THREADS=4

# Postgres connection pool (solo cuando DATABASE_URL está configurada).
# RUANA_DB_POOL_MIN=1
# RUANA_DB_POOL_MAX=5

# Cron operativo (Cloud Scheduler): ver docs/operaciones/cloud_scheduler_jobs.md
# RUANA_CRON_SECRET=<valor-aleatorio-largo>

FLASK_SECRET_KEY=change-this-to-a-long-random-value
RUANA_ADMIN_SESSION_EXPIRES=3600
RUANA_ALIADO_SESSION_EXPIRES=3600

# Credenciales de administrador (fuera del repositorio).
# Opción A: ruta a un JSON con hashes (recomendado en local/producción).
# RUANA_ADMIN_CREDENTIALS_PATH=.local-secrets/admin_credentials.json
# Opción B: JSON inline para Cloud Run u otros despliegues sin volumen.
# RUANA_ADMIN_CREDENTIALS_JSON={"version":1,"admins":{...}}
# Migración inicial desde admin_codes.json legado:
#   python RUANA/scripts/bootstrap_admin_credentials.py --legacy RUANA/config/admin_codes.json
# Subir a Cloud Run (requiere gcloud autenticado):
#   npm run gcp:admin-credentials
# Producción recomendada (sin gcloud): ver docs/seguridad/credenciales-admin.md
#   python RUANA/scripts/generate_github_admin_secret.py --admin-id 7772735 --password '...'
#   → GitHub Secret RUANA_ADMIN_CREDENTIALS_JSON → push main

FIREBASE_PROJECT_ID=ruana-4293f
GOOGLE_CLOUD_REGION=europe-west1
ARTIFACT_REGISTRY_REPOSITORY=ruana

SUPABASE_URL=https://qqlxgwbmtzcfrrobrfzy.supabase.co
SUPABASE_ANON_KEY=your-public-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-secret-service-role-key

# Prefer Supabase Transaction Pooler for Cloud Run/serverless.
# Example format:
# postgresql://postgres.<project-ref>:<password>@aws-0-eu-west-1.pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres.<project-ref>:<password>@<pooler-host>:6543/postgres

# Local SQLite fallback while the Postgres adapter is being migrated.
RUANA_DB_PATH=./RUANA/ruana.db

# Correo transaccional (bienvenida con código de aliado al registrarse).
# Gmail: usar contraseña de aplicación (https://myaccount.google.com/apppasswords).
# Producción (GitHub Actions): crear secret RUANA_SMTP_PASSWORD en el repositorio.
# Local / gcp:secrets: definir aquí o en .env.local.
RUANA_SMTP_HOST=smtp.gmail.com
RUANA_SMTP_PORT=587
RUANA_SMTP_USER=team.ruana@gmail.com
RUANA_SMTP_PASSWORD=your-gmail-app-password
# Opcional; por defecto usa RUANA_SMTP_USER.
# RUANA_SMTP_FROM_EMAIL=team.ruana@gmail.com

# Stripe Connect (pagos encargo: separate charges and transfers)
# RUANA_STRIPE_PAYMENTS_ENABLED=1
# RUANA_STRIPE_MODE=test
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_API_VERSION=2024-11-20.acacia
# RUANA_STRIPE_TRANSFER_TIMEOUT_DAYS=12
